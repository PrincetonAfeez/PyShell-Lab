# Abstract development-tool requirements (mirrors the pyproject "dev" extra).
# The pinned lock lives in requirements-dev.txt. Regenerate it with:
#   python -m piptools compile requirements-dev.in -o requirements-dev.txt --strip-extras
pytest>=8,<9
pytest-cov>=5,<7
ruff>=0.6,<1.0
mypy>=1.11,<2.0
